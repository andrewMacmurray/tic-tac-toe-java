# Tic Tac Toe Java
Command line version of Tic Tac Toe in Java

### To play the game:

Build the project using:

```sh
> ./gradlew build
```

Then run using:

```sh
> ./gradlew run --console=plain
```